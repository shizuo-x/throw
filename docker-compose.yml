version: '3.8'

services:
  app:
    build: .
    ports:
      - "3009:3009"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - PORT=3009
      - BASE_URL=http://localhost:3009
    restart: unless-stopped